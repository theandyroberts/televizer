{
  "name": "televizer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@televizer/core && npm run build --workspace=@televizer/demo",
    "dev": "npm run dev --workspace=@televizer/demo",
    "test": "npm run test --workspace=@televizer/core",
    "typecheck": "npm run typecheck --workspace=@televizer/core && npm run typecheck --workspace=@televizer/demo"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "jsdom": "^29.1.1"
  },
  "license": "MIT"
}
